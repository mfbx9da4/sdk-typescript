/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsFieldsGet } from "../../funcs/documentsFieldsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.FieldGetDocumentFieldRequest$inboundSchema,
};

export const tool$documentsFieldsGet: ToolDefinition<typeof args> = {
  name: "documents-fields_get",
  description: `Get document field

Returns a single field. If you want to retrieve all the fields for a document, use the "Get Document" endpoint.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsFieldsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
