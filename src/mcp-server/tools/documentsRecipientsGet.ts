/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsRecipientsGet } from "../../funcs/documentsRecipientsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RecipientGetDocumentRecipientRequest$inboundSchema,
};

export const tool$documentsRecipientsGet: ToolDefinition<typeof args> = {
  name: "documents-recipients_get",
  description: `Get document recipient

Returns a single recipient. If you want to retrieve all the recipients for a document, use the "Get Document" endpoint.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsRecipientsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
