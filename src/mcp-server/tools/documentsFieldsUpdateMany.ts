/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsFieldsUpdateMany } from "../../funcs/documentsFieldsUpdateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.FieldUpdateDocumentFieldsRequestBody$inboundSchema,
};

export const tool$documentsFieldsUpdateMany: ToolDefinition<typeof args> = {
  name: "documents-fields_update-many",
  description: `Update document fields

Update multiple fields for a document.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsFieldsUpdateMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
