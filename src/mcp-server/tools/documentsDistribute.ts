/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsDistribute } from "../../funcs/documentsDistribute.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentSendDocumentRequestBody$inboundSchema,
};

export const tool$documentsDistribute: ToolDefinition<typeof args> = {
  name: "documents_distribute",
  description: `Distribute document

Send the document out to recipients based on your distribution method`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsDistribute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
