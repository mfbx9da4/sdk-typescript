/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsRecipientsDelete } from "../../funcs/documentsRecipientsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RecipientDeleteDocumentRecipientRequestBody$inboundSchema,
};

export const tool$documentsRecipientsDelete: ToolDefinition<typeof args> = {
  name: "documents-recipients_delete",
  description: `Delete document recipient`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsRecipientsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
