/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsDuplicate } from "../../funcs/documentsDuplicate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentDuplicateDocumentRequestBody$inboundSchema,
};

export const tool$documentsDuplicate: ToolDefinition<typeof args> = {
  name: "documents_duplicate",
  description: `Duplicate document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsDuplicate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
