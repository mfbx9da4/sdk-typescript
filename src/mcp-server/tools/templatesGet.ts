/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { templatesGet } from "../../funcs/templatesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TemplateGetTemplateByIdRequest$inboundSchema,
};

export const tool$templatesGet: ToolDefinition<typeof args> = {
  name: "templates_get",
  description: `Get template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await templatesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
