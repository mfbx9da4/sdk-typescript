/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { templatesMoveToTeam } from "../../funcs/templatesMoveToTeam.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TemplateMoveTemplateToTeamRequestBody$inboundSchema,
};

export const tool$templatesMoveToTeam: ToolDefinition<typeof args> = {
  name: "templates_move-to-team",
  description: `Move template

Move a template to a team`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await templatesMoveToTeam(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
