/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { templatesRecipientsCreateMany } from "../../funcs/templatesRecipientsCreateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.RecipientCreateTemplateRecipientsRequestBody$inboundSchema,
};

export const tool$templatesRecipientsCreateMany: ToolDefinition<typeof args> = {
  name: "templates-recipients_create-many",
  description: `Create template recipients

Create multiple recipients for a template.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await templatesRecipientsCreateMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
