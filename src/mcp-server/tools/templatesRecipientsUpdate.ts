/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { templatesRecipientsUpdate } from "../../funcs/templatesRecipientsUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RecipientUpdateTemplateRecipientRequestBody$inboundSchema,
};

export const tool$templatesRecipientsUpdate: ToolDefinition<typeof args> = {
  name: "templates-recipients_update",
  description: `Update template recipient

Update a single recipient for a template.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await templatesRecipientsUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
