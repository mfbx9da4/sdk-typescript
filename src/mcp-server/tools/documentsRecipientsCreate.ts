/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsRecipientsCreate } from "../../funcs/documentsRecipientsCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.RecipientCreateDocumentRecipientRequestBody$inboundSchema,
};

export const tool$documentsRecipientsCreate: ToolDefinition<typeof args> = {
  name: "documents-recipients_create",
  description: `Create document recipient

Create a single recipient for a document.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsRecipientsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
