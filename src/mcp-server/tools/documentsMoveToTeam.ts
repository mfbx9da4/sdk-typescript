/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsMoveToTeam } from "../../funcs/documentsMoveToTeam.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentMoveDocumentToTeamRequestBody$inboundSchema,
};

export const tool$documentsMoveToTeam: ToolDefinition<typeof args> = {
  name: "documents_move-to-team",
  description: `Move document

Move a document from your personal account to a team`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsMoveToTeam(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
