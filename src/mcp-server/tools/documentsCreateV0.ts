/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsCreateV0 } from "../../funcs/documentsCreateV0.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentCreateDocumentTemporaryRequestBody$inboundSchema,
};

export const tool$documentsCreateV0: ToolDefinition<typeof args> = {
  name: "documents_create-v0",
  description: `Create document

You will need to upload the PDF to the provided URL returned. Note: Once V2 API is released, this will be removed since we will allow direct uploads, instead of using an upload URL.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsCreateV0(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
